PORT=3000
ADMIN_TOKEN=change-me-to-a-real-secret
# Set to "true" (and restart) to take the whole site offline — every request
# returns 404, Next.js never initializes. Remove/unset to restore normal service.
SITE_LOCKED=false
